using System;
using System.IO;
using System.Text.Json;

namespace PracticeNotebook.LINQ
{
    public class LinqExpression
    {
        private const string filePath = @"/Users/shari/Documents/Dotnet/PracticeNotebook/PracticeNotebook.LINQ/rules.json";
        public void ReadFile()
        {
            // var jsonStr = File.ReadAllText(filePath);
            // var jsonDocument = JsonDocument.Parse(jsonStr);
            // Console.WriteLine(jsonDocument);
            //var name = nameof(filePath);
        }
    }
}